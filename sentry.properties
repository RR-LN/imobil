# Sentry Configuration for KuGava
# Get these values from https://sentry.io/settings/account/api/auth-tokens/

defaults.url=https://sentry.io
defaults.org=your-org-name
defaults.project=kugava-app

# Auth token for releasing
# Create one at: https://sentry.io/settings/account/api/auth-tokens/
# Required scopes: project:read, project:write, project:releases
# auth.token=your-auth-token-here
